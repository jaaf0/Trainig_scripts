subenum.py is not fast and useless but for super fater tool we used Asynchronous requests
with subsuper.py
so you should check if you have "aiohttp" installed

python3 -m venv myenv
source myenv/bin/activate
pip install aiohttp aiodns tqdm
 run the script
